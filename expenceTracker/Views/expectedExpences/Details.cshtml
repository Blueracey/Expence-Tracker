@model expenceTracker.Models.expectedExpences

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>expectedExpences</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Name
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.name)
        </dd>
        <dt class="col-sm-2">
            Type
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.type)
        </dd>
        <dt class="col-sm-2">
            Frequency
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.frequency)
        </dd>
        <dt class="col-sm-2">
           Predicted Cost
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.predictedCost)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>


    @* TRYING THIS OUT *@
    <a asp-controller="actualExpences" asp-action="Create" asp-route-expenceId="@Model?.expenceId" asp-route-userId="@Model?.userId" asp-route-category="@Model?.name">Create Actual Expence</a>


    <a asp-controller="expectedExpences" asp-action="Index" asp-route-expenceId="@TempData.Peek("expenceId")" asp-route-userId="@TempData.Peek("userId")">Back</a>
</div>
